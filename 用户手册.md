# 拓漫 TouMan — 用户手册

## 安装

打开 PowerShell（右键 → 以管理员身份运行），粘贴以下命令：

```powershell
iex (irm https://raw.githubusercontent.com/eren11qq/tuoman-sales/main/scripts/tuoman-install.ps1)
```

安装完成后，桌面会出现「拓漫」图标。

---

## 配置 API Key

首次使用需要配置 AI 模型的 API Key。

创建文件 `%LOCALAPPDATA%\hermes\.env`，写入：

```env
# 至少配置一个
OPENAI_API_KEY=sk-你的key
DEEPSEEK_API_KEY=sk-你的key
```

---

## 启动

**方式一：** 双击桌面「拓漫」图标
**方式二：** 打开终端运行：

```powershell
cd %LOCALAPPDATA%\tuoman
.venv\Scripts\python cli.py
```

---

## 基本使用

启动后看到 `❯` 提示符，输入指令：

| 你说 | 它做什么 |
|------|---------|
| `找客户` | 搜索 AI 漫剧创作者线索 |
| `调研 灵境AI` | 深度调研某家公司 |
| `/help` | 查看所有命令 |
| `/model` | 切换 AI 模型 |

### 获客流程

```
找客户 → 自动搜索各平台
  ↓
调研线索 → 深度分析公司情况
  ↓
打分排序 → 标记 HOT/WARM/COLD
  ↓
生成话术 → 自动写触达文案
  ↓
每日报告 → 汇总当天进展
```

---

## 定时任务（可选）

每天早上 9 点自动跑获客管线：

```powershell
.\scripts\setup_scheduler.ps1
```
